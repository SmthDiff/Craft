<head>
    <meta charset="UTF-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="referrer" content="origin-when-cross-origin" />
    <title>{{ entry.title }} - {{ currentSite.name }}</title>
    {{ include('_layout/head/meta.twig') }}
    {{ head() }}
    {{ include('_layout/head/styles.twig') }}
    {{ include('_layout/head/fonts.twig') }}
    {{ include('_layout/head/icons.twig') }}
    {{ include('_layout/head/theme.twig') }}
    {{ include('_layout/head/scripts.twig') }}
</head>
